{
    "name": "anonymous-question-asking-platform",
    "version": "1.0.0",
    "scripts": {
        "start": "node server.js",
        "build": "cd frontend && npm install && npm run build",
        "dev": "npm-run-all --parallel \"cd frontend && npm run dev\" \"npm start\""
    },
    "dependencies": {
        "cors": "^2.8.5",
        "express": "^4.18.2",
        "socket.io": "^4.8.1"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5"
    }
}
